// config.js - Global Configuration for PhishGuard Extension
// Update these URLs before deploying to production.

const CONFIG = {
    // The base URL of the FastAPI backend (e.g., https://phishguard-backend.onrender.com)
    API_BASE_URL: "http://127.0.0.1:8000",
    
    // The URL of the React Dashboard (e.g., https://phishguard-dashboard.vercel.app)
    DASHBOARD_URL: "http://localhost:5173"
};
